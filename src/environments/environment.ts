// This file is the default environment for development.
// It can be overridden by environment.local.ts (if present) for local dev.

export const environment = {
  production: false,
  apiUrl: 'https://uselessfacts.jsph.pl/random.json'
};
